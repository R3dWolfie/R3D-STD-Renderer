# venv / python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# test data — never commit maps/replays/skins/audio
*.osu
*.osr
*.osk
*.osz
*.mp3
*.ogg
*.wav
*.mp4
*-skin/
test-data/

# Bundled osu! DEFAULT nightcore drums are a SHIPPED asset (final fallback for
# the NC-mod beat overlay), not test output — keep them tracked despite *.wav.
!osu_std_renderer/assets/default_nightcore/
!osu_std_renderer/assets/default_nightcore/*.wav

# scratch
*.png
!docs/*.png
.tmp/
.ruff_cache/
*.bak*
*backup*

# R3D logo — license-clean own IP; keep it in-repo so clones do not fall back to the placeholder tile
!osu_std_renderer/assets/logo.png
